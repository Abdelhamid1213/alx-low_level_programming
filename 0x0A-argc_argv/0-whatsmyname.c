#include "main.h"
#include <stdio.h>

/**
 * main - Entry point
 *
 * @argc: number
 * @argv: array of strings
 *
 * Return: 0 Always success.
 */

int main(int __attribute__((unused)) argc, char *argv[])
{
	int i = 0;

	printf("%s\n", argv[i]);

	return (0);
}
